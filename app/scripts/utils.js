const utils =  {
  //ask about the below syntx- is this an object with a function in it? 
  copystate: (oldState, newState)=>{
    return Object.assign({}, oldState, newState);
  }
}

export default utils
